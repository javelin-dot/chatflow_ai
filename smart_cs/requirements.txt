# smart_cs 阶段 0 依赖：只保留跑通 hello-world 必需的轻量包。
# 阶段 1 起会按需追加 LLM / 向量检索 / FastAPI / DB 驱动。

# CLI
click>=8.0.0,<9.0.0

# YAML 配置
pyyaml>=6.0.0,<7.0.0

# 可选：HTTP API（不装也能跑 shell）
fastapi>=0.100.0,<1.0.0
uvicorn>=0.20.0,<1.0.0

# 可选：日志/类型工具
typing-extensions>=4.0.0,<5.0.0
